{
  "name": "saxon-scout",
  "version": "1.0.0",
  "description": "A local-first scouting platform for FRC events",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run build:app:watch\" \"npm run server:dev\"",
    "build": "npm run clean && npm run build:server && npm run build:app",
    "build:server": "tsc -p server/tsconfig.json",
    "build:app": "node scripts/build-app.js",
    "build:app:watch": "node scripts/build-app.js --dev",
    "clean": "node scripts/clean.js",
    "start": "node dist/server/server.js",
    "server:dev": "node --watch dist/server/server.js",
    "test": "node server/test-server.js",
    "lint": "eslint app/src server/src --ext .ts,.tsx"
  },
  "dependencies": {
    "inferno": "^8.2.0",
    "bootstrap": "^5.3.0",
    "jszip": "^3.10.1",
    "chart.js": "^4.4.0",
    "express": "^4.18.2",
    "cookie-parser": "^1.4.6",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3",
    "concurrently": "^8.2.2"
  }
}